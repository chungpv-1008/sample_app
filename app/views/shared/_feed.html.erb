<% if @feed_items.any? %>
  <%= paginate @feed_items, params: {controller: :static_pages, action: :home} %>
    <ol class="microposts">
      <%= render @feed_items %>
    </ol>
  <%= paginate @feed_items %>
<% end %>
