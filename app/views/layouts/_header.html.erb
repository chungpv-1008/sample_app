<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <%= link_to t("layouts._header.sample_app"), root_path, id: "logo", class: "navbar-brand" %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
        <%= link_to t("layouts.application.home"), root_path, class: "nav-link" %>
      </li>
      <li class="nav-item">
        <%= link_to t("layouts.application.help"), help_path, class: "nav-link" %>
      </li>
      <% if logged_in? %>
        <li class="nav-item">
          <%= link_to t("layouts._header.users"), "#", class: "nav-link" %>
        </li>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            <%= t "layouts._header.account" %><b class="caret"></b>
          </a>
          <div class="dropdown-menu" aria-labelledby="navbarDropdown">
            <%= link_to t("layouts._header.profile"), current_user, class: "dropdown-item" %>
            <%= link_to t("layouts._header.settings"), "#", class: "dropdown-item" %>
            <%= link_to t("layouts._header.log_out"), logout_path, method: :delete, class: "dropdown-item" %>
          </div>
        </li>
      <% else %>
        <li><%= link_to t("layouts._header.log_in"), login_path, class: "nav-link" %></li>
      <% end %>
    </ul>
  </div>
</nav>
